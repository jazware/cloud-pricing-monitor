global:
  scrape_interval: 60s
  evaluation_interval: 60s

scrape_configs:
  - job_name: 'cloud-pricing-monitor'
    static_configs:
      - targets: ['cloud-pricing-monitor:9090']
        labels:
          service: 'cloud-pricing-monitor'