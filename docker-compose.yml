services:
  cloud-pricing-monitor:
    build: .
    ports:
      - "6015:6015"
    env_file: ".env"
    volumes:
      # Uncomment and adjust path to your GCP service account key
      - ./secrets/gcp-creds.json:/app/gcp-creds.json
    restart: unless-stopped